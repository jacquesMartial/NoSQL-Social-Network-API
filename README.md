# NoSQL-Social-Network-API
This app uses Express.js for routing, a MongoDB database, the Mongoose ODM and a JavaScript date library to build an API for a social network web application where users can share their thoughts, react to friends’ thoughts, and create a friend list.
